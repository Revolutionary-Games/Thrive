// External declarations for everything

namespace BaseMicrobeOperations{

external shared SpeciesComponent@ getSpeciesComponent(const string &in speciesName);
external shared ProcessorComponent@ getProcessorComponent(const string &in speciesName);
}
