using System;

public static class PatchMapGenerator
{
    public static PatchMap Generate(WorldGenerationSettings settings)
    {
        var map = new PatchMap();

        // TODO: copy over the default patch map

        return map;
    }
}
