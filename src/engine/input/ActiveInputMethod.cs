public enum ActiveInputMethod
{
    /// <summary>
    ///   Keyboard and mouse is used for input
    /// </summary>
    Keyboard,

    /// <summary>
    ///   A controller (game pad) is used for input
    /// </summary>
    Controller,
}
