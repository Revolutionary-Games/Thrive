using Godot;

/// <summary>
///   Handles displaying the patch notes from the previous version to the current one
/// </summary>
public class PatchNotesDisplayer : VBoxContainer
{
    public override void _Ready()
    {
    }

    /// <summary>
    ///   Triggered when the button to show something is pressed, will show the latest release's patch notes
    /// </summary>
    private void ShowLatestPatchNotesIfNotVisible()
    {
    }
}
