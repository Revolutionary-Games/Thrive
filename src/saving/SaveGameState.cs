/// <summary>
///   A game state the save was made in.
/// </summary>
/// <remarks>
///   <para>
///     When adding new values only add them at the end to not break existing saves
///   </para>
/// </remarks>
public enum SaveGameState
{
    Invalid,
    MicrobeStage,
    MicrobeEditor,
}
